/*import { configureStore } from '@reduxjs/toolkit';
import workoutReducer from './workoutSlice'; // Ensure the correct import path
import userReducer from './userSlice'; // Ensure the correct import path

const store = configureStore({
  reducer: {
    workout: workoutReducer,
    auth: userReducer,
  },
});

export default store;*/
